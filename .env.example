# OpenAI API Key for chatbot responses
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=ask-your-openai-api-key

# Supabase Database Configuration
# For development (IPv6 only):
#DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# For production/Vercel (IPv4 compatible):
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[DATABASE-PASSWORD]@aws-1-[REGION].pooler.supabase.com:5432/postgres?sslmode=require&application_name=boralio_chatbot

# Local development (if needed):
#DATABASE_URL=postgresql://postgres:password@localhost:5432/lead_scoring_chatbot

# Mailgun Domain (for sending email notifications)
# Get from: Mailgun Dashboard > Sending > Domains
# Example: mg.yourdomain.com or sandbox123.mailgun.org
MAILGUN_DOMAIN=your-mailgun-domain.com

# Mailgun API Key (for authentication)
# Get from: Mailgun Dashboard > Settings > API Keys
MAILGUN_API_KEY=your-mailgun-api-key

# Email address to send notifications FROM
# This should be an authorized sender in your Mailgun domain
# Example: noreply@yourdomain.com
EMAIL_FROM=noreply@yourdomain.com

# Email address to send lead notifications TO
# This is where you'll receive qualified lead notifications
EMAIL_TO=your-email@example.com